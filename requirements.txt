# ============================================================================
# DASHBOARD-TRONIK - Dependencias Python
# ============================================================================
# Instalacao:
#   pip install -r requirements.txt
#
# Verificacao:
#   python -c "import flask, sqlalchemy, pydantic; print('ok')"
# ============================================================================

# --- Web framework ----------------------------------------------------------
Flask>=3.1.3,<4.0
Flask-CORS>=6.0.0,<7.0
Werkzeug>=3.1.6,<4.0
gunicorn>=23.0.0,<24.0

# --- Banco de dados ---------------------------------------------------------
SQLAlchemy>=2.0.23
# psycopg v3 (suporta Python 3.13+; substituto de psycopg2-binary)
psycopg[binary]==3.2.13

# --- Auth / rate limiting / seguranca --------------------------------------
Flask-Login==0.6.3
Flask-Limiter==3.5.0
Flask-Talisman==1.1.0

# --- Validacao / contratos de API ------------------------------------------
# Pydantic v2 e o contrato oficial dos payloads de endpoint (ver banco_dados/contratos.py).
pydantic>=2.6,<3.0

# --- Notificacoes / agendamento --------------------------------------------
Flask-Mail==0.9.1
APScheduler==3.10.4

# --- Realtime ---------------------------------------------------------------
Flask-SocketIO==5.3.6

# --- Utilidades -------------------------------------------------------------
python-dateutil==2.8.2
python-dotenv>=1.2.2,<2.0
requests>=2.32.4,<3.0
openai>=1.0.0
reportlab==4.0.7
pypdf>=4.0,<6.0

# --- Machine Learning -------------------------------------------------------
numpy>=2.0,<3.0
scikit-learn>=1.4,<2.0
xgboost>=2.0,<3.0
joblib>=1.3,<2.0
# statsforecast>=1.7,<2.0  # OPCIONAL: requer MSVC Build Tools no Windows
#   pip install statsforecast  (se tiver Visual C++ 14.0+)
#   Sem ele, ml_predicao usa fallback de regressão linear (funciona bem)


# --- Desenvolvimento e testes ----------------------------------------------
pytest>=8.3.3,<9.0
pytest-flask>=1.3.0
pytest-cov==4.1.0

# Lint e format (opcional em dev; pre-commit usa as mesmas versoes):
# ruff>=0.5.0
# black>=24.3.0

# --- Observabilidade (opcional; requer SENTRY_DSN no .env) ------------------
# sentry-sdk[flask]>=2.0

